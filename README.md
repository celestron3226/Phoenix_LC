# Phoenix_LC

## Environment setup

​```bash
conda env create -f environment_pytorch_gpu.yml   # For model training and predicting
conda env create -f environment_geopandas.yml     # For all other scripts
​```
